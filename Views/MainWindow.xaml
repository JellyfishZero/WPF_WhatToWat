<Window x:Class="WhatToEat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhatToEat"
        mc:Ignorable="d"
        Title="今天吃什麼？" Height="450" Width="800" MinHeight="420" MinWidth="640">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="工具">
                <MenuItem Header="新增餐廳" Click="AddItemClick"/>
                <MenuItem Header="修改餐廳" Click="ModifyItemClick"/>
                <Separator/>
                <MenuItem Header="查看所有餐廳" Click="QueryItemClick"/>
                <Separator/>
                <MenuItem Header="刪除餐廳" Click="DeleteItemClick"/>
            </MenuItem>
        </Menu>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="今天吃什麼？"
                               FontSize="22"
                               FontWeight="SemiBold" />
                    <TextBlock Text="依照現在時間，從營業中的餐廳抽出一間。"
                               Foreground="Gray"
                               TextWrapping="Wrap"
                               Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="24,0,0,0">
                    <TextBlock Text="現在時間"
                               Foreground="Gray"
                               HorizontalAlignment="Right" />
                    <TextBlock Text="{Binding CurrentTimeText}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Right"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <TextBlock Grid.Row="1"
                       Text="{Binding StatusMessage}"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16" />

            <Border Grid.Row="2"
                    BorderBrush="#FFE5E5E5"
                    BorderThickness="1"
                    Background="#FFF8F8F8"
                    Padding="24"
                    Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="1"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock Text="抽選結果"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8" />
                        <TextBlock Text="{Binding SelectedRestaurantName}"
                                   FontSize="36"
                                   FontWeight="SemiBold"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   MaxWidth="560" />
                        <TextBlock Text="{Binding SelectedRestaurantNote}"
                                   Foreground="Gray"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   MaxWidth="560"
                                   Margin="0,12,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="可用上方工具新增、修改、查看或刪除餐廳。"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,16,0" />

                <Button Grid.Column="1"
                        Content="抽一家店"
                        Width="120"
                        Height="36"
                        HorizontalAlignment="Right"
                        Command="{Binding DrawRestaurantCommand}" />
            </Grid>
        </Grid>
    </DockPanel>
</Window>
